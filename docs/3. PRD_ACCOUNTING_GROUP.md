# PRD: Accounting Group (Accounting + Invoicing)
**Status**: Kustomasi Minor  
**Owner Group**: Accounting Team  
**Referensi**: BPMN Scenario 1 (Course setup mode), Scenario 2 (Payment Integration), Scenario 3 (Hybrid check-in boundary), Scenario 4 (Certificate)  
**Tanggal**: 2026  
**Version**: 2.0 — Revisi Faktual (Odoo 19)

---

## 1. Executive Summary

Accounting Group bertanggung jawab untuk **payment processing dan financial reconciliation** dalam platform Microcredential IMPC. 

> **Prinsip Odoo 19**: Payment gateway (Midtrans/Xendit) tersedia sebagai modul third-party di Odoo Apps Store — **bukan custom development dari nol**. Tugas utama Accounting Group adalah menginstal, mengkonfigurasi, dan mengintegrasikan modul tersebut, serta membangun reconciliation logic untuk Redeem Code B2B.

**Deliverables** (Kustomasi Minor — Install + Config + Minor Code):
- Instalasi & konfigurasi modul payment provider Midtrans/Xendit dari Odoo Apps (cek kompatibilitas v19)
- Konfigurasi Indonesian Localization + PPN 11%
- Enrollment trigger ke Website Group setelah pembayaran dikonfirmasi
- Redeem code transaction reconciliation untuk B2B revenue tracking
- Invoice metadata enrichment (learning_mode per line item)

**Deliverables** (Standar — Native Odoo 19):
- Invoice generation (account.invoice native)
- Payment state management
- Financial reporting
- Ledger entries & audit trail

**Catatan Penting**: Accounting Group TIDAK membangun payment gateway dari nol. Model `payment.provider` dan `payment.transaction` sudah ada di Odoo 19 secara native dan diextend oleh modul third-party Midtrans/Xendit.

---

## 2. Scope & Boundaries

### 2.1 Scope: APA YANG ACCOUNTING GROUP KERJAKAN

**Accounting Module**:
- ✅ Instalasi & konfigurasi modul third-party Midtrans/Xendit dari Odoo Apps Store
- ✅ Konfigurasi Indonesian Localization + PPN 11% (Accounting → Settings → Fiscal Localization)
- ✅ Konfigurasi payment provider di Accounting → Configuration → Payment Providers
- ✅ Enrollment trigger setelah invoice `payment_state='paid'`
- ✅ Redeem code transaction reconciliation (B2B revenue tracking)
- ✅ Invoice generation untuk B2C & B2B (native)
- ✅ Financial reporting & audit trail (native)

**Invoicing Module**:
- ✅ Invoice generation from sales orders
- ✅ Payment state management
- ✅ Invoice templates for course sales
- ✅ Invoice delivery to customers

### 2.2 Boundary: APA YANG TIDAK ACCOUNTING GROUP TANGANI

**❌ BOUNDARY - Website Group Handle**:
- Course enrollment decision (Website decides after payment confirmation)
- Redeem code validation logic (Website validates codes)
- Redeem code generation (Website creates codes)
- E-ticket generation, scan check-in, attendance status, dan exam gating (Website owns)
- Certificate generation (Website creates certificate)

**❌ BOUNDARY - Sales Group Handle**:
- B2B contract negotiation & terms
- Pricing strategy & discount negotiation
- Customer account management

**❌ BOUNDARY - HR Group Handle**:
- Employee learning records
- Training budget allocation (business logic)

---

## 3. Feature Requirements

### 3.0 Invoice Metadata for Learning Mode (SKENARIO 1)

#### User Story 0: Persist Course Learning Mode in Billing Records

**Given**: Course memiliki mode `ONLINE`, `OFFLINE`, atau `HYBRID`  
**When**: Invoice dibuat dari sale.order  
**Then**: Metadata mode course tersimpan untuk kebutuhan audit/rekonsiliasi

**Acceptance Criteria**:
- [ ] Invoice line menyimpan `learning_mode` course saat transaksi
- [ ] Mode ditampilkan di PDF invoice sebagai informasi non-finansial
- [ ] Tidak ada perubahan perhitungan harga berbasis mode di Accounting (harga tetap dari Sales/Website)
- [ ] Report keuangan dapat difilter berdasarkan mode course

**Database Schema**:
```
account.invoice.line (extended):
  - course_id (FK to elearning.course, nullable)
  - learning_mode (selection: 'ONLINE', 'OFFLINE', 'HYBRID', nullable)
```

---

### 3.1 Payment Gateway Integration (SKENARIO 2: B2C Payment)

> **Pendekatan Odoo 19**: Gunakan modul third-party Midtrans atau Xendit dari Odoo Apps Store. Verifikasi kompatibilitas dengan Odoo 19.0 sebelum instalasi di production. Modul ini mengextend model native `payment.provider` dan `payment.transaction` Odoo — tidak perlu membuat model payment baru dari nol.

#### User Story 1: Instalasi & Konfigurasi Payment Provider

**Given**: Admin perlu mengaktifkan metode pembayaran untuk platform  
**When**: Admin menginstal dan mengkonfigurasi modul Midtrans/Xendit  
**Then**: Pembayaran online aktif dengan Virtual Account, QRIS, Alfamart/Indomaret

**Acceptance Criteria**:
- [ ] Instal modul third-party (Midtrans atau Xendit) dari Odoo Apps Store (bukan custom dev)
- [ ] Verifikasi kompatibilitas modul dengan Odoo 19.0 di staging sebelum production
- [ ] Konfigurasi di: Accounting → Configuration → Payment Providers
- [ ] Input credentials: Server Key, Client Key (mode Test & Production terpisah)
- [ ] Aktifkan metode pembayaran: Virtual Account, QRIS, Alfamart/Indomaret
- [ ] Test payment di staging environment sebelum go-live
- [ ] Webhook URL (disediakan modul third-party) didaftarkan ke dashboard Midtrans/Xendit

**Catatan Implementasi**:
- Model yang digunakan: `payment.provider` (native Odoo, diextend modul third-party)
- Webhook handling: disediakan oleh modul third-party (tidak perlu custom)
- Credentials: disimpan di field native `payment.provider` (encrypted oleh Odoo)
- **TIDAK PERLU**: membuat model `payment.gateway` baru atau custom webhook endpoint

**Konfigurasi (bukan kode)**:
```
Accounting → Configuration → Payment Providers → [Midtrans/Xendit]
  - Name: Midtrans
  - State: Enabled
  - Mode: Test (staging) / Production
  - Server Key: [dari dashboard Midtrans]
  - Client Key: [dari dashboard Midtrans]
  - Webhook URL: [otomatis dibuat oleh modul]
  - Payment Methods: VA BCA, VA BNI, QRIS, Alfamart
```

---

#### User Story 2: Konfigurasi Enrollment Trigger Post-Payment

**Given**: Pembayaran kursus B2C dikonfirmasi oleh payment provider  
**When**: `account.move` (invoice) `payment_state` berubah ke `'paid'`  
**Then**: Accounting memicu enrollment peserta ke Website Group

> **Ini adalah satu-satunya custom code** yang diperlukan di sisi Accounting. Webhook dari payment provider (Midtrans/Xendit) ke Odoo sudah ditangani oleh modul third-party secara native.

**Acceptance Criteria**:
- [ ] Automated Action (Settings → Technical → Automation) trigger pada `account.move` saat `payment_state='paid'`
- [ ] Filter: hanya invoice dengan `invoice_line_ids.course_id != False` (kursus eLearning)
- [ ] Action: POST ke endpoint Website Group `/api/v1/elearning/enrollment-trigger`
- [ ] Idempotency: Gunakan `invoice.id` sebagai key — satu invoice hanya trigger enrollment satu kali
- [ ] Retry: Jika Website Group endpoint tidak respond dalam 10 detik, retry max 3x dengan exponential backoff
- [ ] Logging: Catat setiap enrollment trigger ke `mail.message` pada invoice (native Odoo chatter)
- [ ] Error: Jika semua retry gagal, buat `mail.activity` pada invoice untuk tindak lanjut manual

**Implementasi** (Automated Action — bukan custom webhook controller):
```python
# Settings → Technical → Automation → Automated Actions
# Model: account.move
# Trigger: When a record is updated
# Before Update Filter: payment_state != 'paid'
# Filter: payment_state = 'paid' AND move_type = 'out_invoice'
# Action: Execute Python Code

for invoice in records:
    course_lines = invoice.invoice_line_ids.filtered(lambda l: l.course_id)
    for line in course_lines:
        # Call Website enrollment API
        payload = {
            'event_type': 'payment_confirmed',
            'invoice_id': invoice.id,
            'course_id': line.course_id.id,
            'customer_id': invoice.partner_id.id,
            'amount': line.price_total,
        }
        env['ir.config_parameter'].sudo()._call_enrollment_api(payload)
```

**API Contract** (Accounting → Website):
```
Endpoint: POST /api/v1/elearning/enrollment-trigger
Authorization: API Key (konfigurasi di ir.config_parameter)
Payload:
{
  "event_type": "payment_confirmed",
  "invoice_id": 12345,
  "course_id": 5,
  "customer_id": 100,
  "amount": 299000,
  "currency": "IDR"
}
Response: {"status": "SUCCESS", "enrollment_id": 50}
```

---

#### User Story 3: Payment Failure Handling

**Given**: Pembayaran gagal (ditangani oleh modul Midtrans/Xendit native)  
**When**: `payment.transaction.state` berubah ke `'error'` atau `'cancel'`  
**Then**: Invoice tetap di status draft; sale.order tidak diproses

**Acceptance Criteria**:
- [ ] Tidak ada custom code diperlukan — modul third-party mengelola failure state
- [ ] `sale.order` tetap di state `'sale'` (confirmed) tapi belum invoiced jika payment gagal
- [ ] Customer dapat retry payment melalui link yang disediakan modul third-party
- [ ] Admin dapat melihat status payment di `Accounting → Payment Providers → Transactions`
- [ ] Log failure tersimpan di native `payment.transaction` record

**Boundary Clarification (Hybrid)**:
- Payment sukses TIDAK otomatis membuka akses ujian akhir pada course hybrid
- Hak buka ujian ditentukan Website Group berdasarkan status check-in Events attendance

---

### 3.2 Invoice Generation from Payment (SKENARIO 2 & 1)

#### User Story 5: Automatic Invoice Generation on Payment

**Given**: Payment confirmed by payment gateway  
**When**: webhook processed and payment_transaction.state='paid'  
**Then**: Invoice auto-generated from sale.order

**Acceptance Criteria**:
- [ ] Invoice trigger: Scheduled action or direct trigger on payment confirmation
- [ ] Invoice source: Created from sale.order (course product)
- [ ] Line items: Course name, price, quantity=1, description
- [ ] Invoice type: Standard "Customer Invoice" (not refund, not credit note)
- [ ] Customer: Invoice customer matched to sale.order customer
- [ ] Amount: Invoice total matches payment amount
- [ ] Payment link: Invoice linked to payment_transaction record
- [ ] Invoice state: Invoice marked as "posted" (not draft)
- [ ] Notification: Invoice sent to customer email
- [ ] Ledger: Accounting entries automatically created (debit receivable, credit revenue)

**Database Schema**:
```
account.invoice (standard Odoo, extended):
  - payment_transaction_id (FK to payment.transaction, nullable)
  - course_id (FK to elearning.course, nullable, for reference)
  - invoice_type (selection: 'COURSE_B2C', 'COURSE_B2B', 'STANDARD')
```

**Workflow**:
```
sale.order (draft) → Payment submitted → payment_transaction (pending)
↓
Payment confirmed → payment_transaction (paid) → Enrollment triggered (Website)
↓
Invoice auto-generated → account.invoice (posted) → Sent to customer
```

---

### 3.3 Redeem Code Reconciliation (SKENARIO 1-2: B2B)

#### User Story 6: Redeem Code Transaction Tracking

**Given**: B2B customer uses redeem code to enroll student  
**When**: Code validated and student enrolled (via Website module)  
**Then**: Transaction recorded for reconciliation

**Acceptance Criteria**:
- [ ] Transaction record: entry in `elearning.redeem_code_transaction`
- [ ] Mapping: Links redeem_code_id, student_id, course_id, enrollment_id
- [ ] Value: Record course price as transaction amount (for reporting)
- [ ] Date: Timestamp of when student claimed code
- [ ] Status: Mark as 'CLAIMED', indicating course slot consumed
- [ ] No payment: Unlike B2C, no actual payment transaction (already paid via contract)
- [ ] Reporting: Transactions aggregatable for B2B revenue reporting

**Database Schema**:
```
elearning.redeem_code_transaction:
  - id (primary)
  - redeem_code_id (FK to redeem_code, indexed)
  - course_id (FK to elearning.course)
  - student_id (FK to res.partner)
  - enrollment_id (FK to course_enrollment)
  - transaction_type (selection: 'CLAIM')
  - amount (float)  # Course price at time of claim
  - currency (char, default 'IDR')
  - claimed_date (datetime)
  - created_at (datetime)
  
account.move (ledger entry, for B2B revenue recognition):
  - redeem_code_transaction_id (FK, nullable)
  - description (e.g., "Course enrollment via redeem: ABC123")
```

---

#### User Story 7: B2B Revenue Recognition Report

**Given**: HR wants to see which redeem codes were used  
**When**: HR runs reconciliation report  
**Then**: Report shows usage per contract

**Acceptance Criteria**:
- [ ] Report availability: Accounting → Reports → "B2B Redeem Code Reconciliation"
- [ ] Filters: By date range, contract, course
- [ ] Columns: Contract #, Course name, Codes allocated, Codes claimed, Codes unused, Expiry date, Revenue amount
- [ ] Export: CSV export functionality
- [ ] Drill-down: Click on contract to see individual student enrollments
- [ ] Auto-calculation: Unused codes value calculated (for contract closeout)

**Report Query**:
```
SELECT 
  contract.name,
  course.name,
  COUNT(CASE WHEN rc.isClaimed=false THEN 1 END) as unused,
  COUNT(CASE WHEN rc.isClaimed=true THEN 1 END) as used,
  SUM(course.price) as revenue,
  rc.expiry_date
FROM elearning.redeem_code rc
JOIN sale.order contract ON rc.contract_id = contract.id
JOIN elearning.course course ON rc.course_id = course.id
GROUP BY contract.id, course.id
```

---

### 3.4 Payment Reconciliation & Matching

#### User Story 8: Payment ↔ Invoice Matching

**Given**: Payment processed and invoice generated  
**When**: Accounting team runs reconciliation  
**Then**: Payment matched to invoice

**Acceptance Criteria**:
- [ ] Automatic matching: System auto-matches payment_transaction to account.invoice based on amount & date
- [ ] Manual matching: If auto-match fails, allow manual reconciliation in UI
- [ ] Status display: Show matched/unmatched payments
- [ ] Reconciliation record: Create `account.reconciliation_item` linking payment + invoice
- [ ] Over/under payment: Alert if payment amount ≠ invoice amount (by > 1% or IDR 1000 threshold)
- [ ] Duplicate detection: Prevent matching same payment to multiple invoices
- [ ] Journal entry: Create reconciliation journal entry in accounting ledger

---

## 4. Integration Points

### 4.1 Accounting ↔ Website Integration

**Use Case**: Payment confirmation → Enrollment trigger

```
┌──────────────────────────┐
│  Payment Gateway         │
│  (External Service)      │
└────────┬─────────────────┘
         │ Webhook: payment.succeeded
         ▼
┌────────────────────────────────────────┐
│  Accounting Payment Handler            │
│  (POST /payment/webhook/stripe)        │
└────────┬──────────────────────────────┘
         │ Parse & verify signature
         │ Update payment_transaction
         ▼
┌────────────────────────────────────────┐
│  Generate Invoice                      │
│  (account.invoice posted)              │
└────────┬──────────────────────────────┘
         │ Enrollment API call (async job)
         ▼
┌────────────────────────────────────────┐
│  Website Enrollment Trigger            │
│  (POST /api/v1/elearning/enroll)       │
└────────────────────────────────────────┘
```

**API Contract** (Accounting → Website):
```
Endpoint: POST /api/v1/elearning/enrollment-trigger
Authorization: Accounting Service User (API Key)
Payload:
{
  "event_type": "payment_confirmed",
  "payment_transaction_id": 12345,
  "sale_order_id": 1000,
  "course_id": 5,
  "customer_id": 100,
  "amount": 299000,
  "currency": "IDR",
  "payment_reference": "ch_1234567890",
  "timestamp": "2026-04-23T10:15:00Z"
}

Response:
{
  "status": "SUCCESS",
  "enrollment_id": 50,
  "message": "Student enrolled successfully"
}

Error Response:
{
  "status": "ERROR",
  "error_code": "STUDENT_ALREADY_ENROLLED",
  "message": "Student sudah terdaftar di course ini"
}
```

### 4.2 Accounting ↔ Sales Integration

**Use Case**: B2B contract reconciliation

```
┌─────────────────────────┐
│  Sales B2B Contract     │
│  (sale.order)           │
└────────┬────────────────┘
         │ Contract approved
         │ → Finance review
         ▼
┌─────────────────────────────────────┐
│  Accounting B2B Contract Approval   │
│  (Financial terms review)           │
└────────┬────────────────────────────┘
         │ Mark as ready for redeem codes
         │ → Signal to Website
         ▼
┌─────────────────────────────────────┐
│  Website Redeem Code Generation     │
│  (Codes created per contract)       │
└─────────────────────────────────────┘
```

---

## 5. Technical Specifications

### 5.1 Development Stack

| Component | Technology | Notes |
|---|---|---|
| **Payment Provider** | Modul Midtrans/Xendit dari Odoo Apps Store | Bukan custom dev — install + config |
| **Enrollment Trigger** | Odoo Automated Action + requests library | Custom: hanya bagian ini |
| **Invoice Extension** | Odoo ORM (inheritance `account.move.line`) | Minor: tambah field `course_id`, `learning_mode` |
| **Reconciliation Report** | Odoo Pivot View + custom SQL query | Untuk B2B redeem code tracking |
| **Async Retry** | Odoo `ir.cron` | Native scheduler untuk retry enrollment trigger |
| **Logging** | Native Odoo chatter (`mail.message`) | Log enrollment trigger per invoice |
| **Testing** | Odoo sandbox + staging environment | Test dengan akun sandbox Midtrans/Xendit |

### 5.2 Security Considerations

- **PCI DSS Compliance**: Tidak ada data kartu kredit tersimpan di Odoo \u2014 semua dihandle oleh Midtrans/Xendit
- **Webhook Signature**: Diverifikasi otomatis oleh modul third-party (tidak perlu implementasi manual)
- **API Keys**: Disimpan di field native `payment.provider` Odoo (encrypted oleh framework)
- **Enrollment Trigger API Key**: Simpan di `ir.config_parameter` (Settings \u2192 Technical \u2192 Parameters \u2192 System Parameters), bukan di kode
- **Audit Trail**: Log enrollment trigger di native Odoo chatter per invoice

### 5.3 Error Handling Strategy

| Error Type | Handling |
|---|---|
| **Connection timeout** | Retry 3x with exponential backoff; mark as "processing" |
| **Invalid amount** | Reject payment, notify customer |
| **Duplicate transaction** | Use idempotency key, return existing result |
| **Gateway down** | Return user-friendly message, preserve order state |
| **Webhook signature invalid** | Log security event, discard webhook |
| **Invoice generation failed** | Trigger alert, allow manual retry |

### 5.4 Performance Requirements

| Feature | Target | Measurement |
|---|---|---|
| Payment processing | < 2s | End-to-end (excluding gateway delay) |
| Webhook processing | < 500ms | Per webhook |
| Invoice generation | < 1s | Per invoice |
| Reconciliation report | < 5s | For 10K transactions |
| Concurrent transactions | 100+ | Per minute |

---

## 6. Acceptance Criteria Checklist

- [ ] Payment gateway integration tested with staging API
- [ ] Webhook signature verification implemented & tested
- [ ] Invoice generation on payment completed & tested
- [ ] B2B redeem code reconciliation working
- [ ] Error handling for all failure scenarios
- [ ] Audit logging comprehensive
- [ ] Integration test with Website module completed
- [ ] Security review passed (no PCI DSS violations)
- [ ] Payment retry logic tested
- [ ] Over/under payment scenarios handled
- [ ] Documentation completed (admin guide, integration guide)

---

## 7. Dependencies & Risks

### Dependencies
- **Modul third-party Midtrans/Xendit** kompatibel dengan Odoo 19.0 (verifikasi sebelum instalasi)
- **Website module** siap menerima enrollment webhook
- **Sales module** contract data tersedia untuk B2B reconciliation

### Risks
| Risk | Probability | Impact | Mitigation |
|---|---|---|---|
| Modul third-party Midtrans/Xendit belum support Odoo 19 | Medium | High | Test di staging; siapkan fallback manual payment confirmation |
| Enrollment trigger gagal setelah payment sukses | Medium | High | Retry logic + mail.activity untuk tindak lanjut manual |
| Duplicate invoice generation | Low | High | Unique constraint pada invoice per payment transaction (native) |
| PCI DSS non-compliance | Very Low | Critical | Seluruh card data di Midtrans/Xendit; tidak ada card data di Odoo |

---

## 8. Deliverables Timeline

| Phase | Deliverables | Duration | Dependencies |
|---|---|---|---|
| **Phase 1** | Payment gateway model + configuration UI | Week 1 | None |
| **Phase 2** | Payment processing integration | Week 2-3 | Phase 1 complete |
| **Phase 3** | Webhook handler + verification | Week 3 | Phase 2 complete |
| **Phase 4** | Invoice generation automation | Week 4 | Phase 3 complete |
| **Phase 5** | B2B redeem code reconciliation | Week 4-5 | Phase 4 complete |
| **Phase 6** | Reporting & reconciliation tools | Week 5-6 | Phase 5 complete |
| **Phase 7** | Testing & integration with Website | Week 6-7 | Website readiness |
| **Phase 8** | Documentation & deployment | Week 7-8 | Phase 7 complete |

---

